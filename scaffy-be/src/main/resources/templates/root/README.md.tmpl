# {{projectName}}

Generated by [Scaffy](https://github.com/filipjoksovic/scaffy) — a project scaffold with backend, frontend and CI wired together.

## Layout

```
{{projectName}}/
├── backend/    # {{backendLabel}}
├── frontend/   # {{frontendLabel}}
└── ...         # CI configuration
```

## Run locally

### Backend

```bash
cd backend
{{backendRunCmd}}
```

The API listens on http://localhost:{{backendPort}}.

### Frontend

```bash
cd frontend
npm install
{{frontendDevCmd}}
```

The dev server listens on http://localhost:{{frontendPort}}.

## Run with Docker

```bash
docker compose up --build
```

| Service  | URL                   |
|----------|-----------------------|
| Backend  | http://localhost:8080 |
| Frontend | http://localhost:80   |
