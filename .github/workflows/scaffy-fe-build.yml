name: Build frontend

on:
  push:
    paths:
      - "scaffy-fe/**"
      - ".github/workflows/scaffy-fe-build.yml"
  pull_request:
    paths:
      - "scaffy-fe/**"
      - ".github/workflows/scaffy-fe-build.yml"

permissions:
  contents: read

jobs:
  build:
    name: Build scaffy-fe
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: scaffy-fe

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: scaffy-fe/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Upload frontend artifact
        uses: actions/upload-artifact@v4
        with:
          name: scaffy-fe-dist
          path: scaffy-fe/dist
          if-no-files-found: error
          retention-days: 7
